// eslint-disable-next-line camelcase
import { Baloo_2, Roboto } from 'next/font/google'

export const roboto = Roboto({
  weight: ['400', '700'],
  subsets: ['latin'],
})

export const baloo700 = Baloo_2({
  weight: '700',
  subsets: ['latin'],
})

export const baloo800 = Baloo_2({
  weight: '800',
  subsets: ['latin'],
})
